<{{ block.settings.tag }} class=”heading” {{ block.shopify_attributes }}>
  Test
</{{ block.settings.tag }}>

{% schema %}
{
  "name": "Test - UtilTitle",
  "tag": null,
  "settings": [
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size",
      "options": [
        {
          "value": "h3",
          "label": "Small"
        },
        {
          "value": "h2",
          "label": "Medium"
        },
        {
          "value": "h1",
          "label": "Large"
        }
      ]
    }
  ],
  "presets": [{ "name": "Test - UtilTitle" }]
}
{% endschema %}
